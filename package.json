{
  "name": "tender-pdf-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "start": "npm --prefix backend install && npm --prefix backend start"
  }
}